name: ESLint

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  eslint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install Dependencies
        run: npm ci

      - name: ESLint Lint
        run: npx eslint:lint
